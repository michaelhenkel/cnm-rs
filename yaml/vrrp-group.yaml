apiVersion: cnm.juniper.net/v1
kind: VrrpGroup
metadata:
  name: vg1
spec:
  vrrpTemplate:
    interfaceSelector:
      interfaceGroupParent:
        name: eth0
    virtualAddress:
      address:
        poolReference:
          name: loopback-pool
    group: 1
