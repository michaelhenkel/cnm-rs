apiVersion: cnm.juniper.net/v1
kind: InterfaceGroup
metadata:
  name: ig1-eth0
spec:
  interfaceSelector:
    list:
    - eth0
  interfaceTemplate:
    managed: true
    instanceParent:
      reference:
        name: crpd1
      parentType: Crpd
